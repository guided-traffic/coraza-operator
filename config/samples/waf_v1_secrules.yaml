apiVersion: waf.gtrfc.com/v1
kind: SecRules
metadata:
  labels:
    app.kubernetes.io/name: coraza-operator
    app.kubernetes.io/managed-by: kustomize
  name: secrules-sample
spec:
  # TODO(user): Add fields here
